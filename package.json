{
  "name": "appsody.dev",
  "private": true,
  "version": "1.0.0",
  "description": "Appsody website",
  "main": "gulpfile.babel.js",
  "engines": {
    "node": "^10.x"
  },
  "dependencies": {
    "@ibm/type": "^0.5.4"
  },
  "scripts": {
    "develop": "npx gatsby develop",
    "build": "npx gatsby build",
    "serve": "npx gatsby serve"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:appsody/website.git"
  },
  "babel": {
    "presets": [
      [
        "env",
        {
          "targets": {
            "browsers": [
              "last 2 versions",
              "safari >= 7"
            ]
          }
        }
      ]
    ]
  },
  "devDependencies": {
    "gatsby-cli": "^2.6.12"
  }
}
